name: Daily Posts Feed

on:
  schedule:
    # Run after the Cogentic producer at 03:30 UTC, with time for queueing and publishing.
    - cron: "30 4 * * *"
  workflow_dispatch:

concurrency:
  group: daily-posts
  cancel-in-progress: false

jobs:
  publish-daily-post:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # Sync runs FIRST: it pulls the real, event-aware quote + the
      # background Cogentic actually rendered today.
      - name: Sync Cogentic AI content
        run: node scripts/sync-cogentic-content.js
        env:
          SITE_URL: https://reallyrealeducation.org

      - name: Generate RSS
        run: node scripts/generate-rss.js
        env:
          SITE_URL: https://reallyrealeducation.org

      - name: Generate post pages
        run: node scripts/generate-post-pages.js
        env:
          SITE_URL: https://reallyrealeducation.org

      - name: Update social meta tags
        run: node scripts/update-social-meta.js
        env:
          SITE_URL: https://reallyrealeducation.org

      - name: Publish latest post to LinkedIn
        run: node scripts/post-to-linkedin.js
        env:
          LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
          LINKEDIN_ORGANIZATION_ID: ${{ secrets.LINKEDIN_ORGANIZATION_ID }}
          LINKEDIN_INCLUDE_POST_URL: "false"

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/posts.json data/linkedin-last-posted.json Cogentic.html posts/ rss.xml
          git diff --cached --quiet || git commit -m "chore: add automated daily post"
          git push
